/* ===== Tomb of Annihilation — Dark Fantasy Theme ===== */
/* Professional flat palette, restrained accents, premium feel */

/* ── Palette ──
   Night Indigo  #0a0d0a   Main backgrounds
   Deep Slate    #121812   Panels, buttons
   Twilight      #1c241c   Hover/selected states
   Frost Line    #2a352a   All borders
   Parchment     #d8c8a8   Primary text
   Mist          #6a7a6a   Secondary text
   Auril's Gold  #d4a017   Headings, gold accents
   Frost Blue    #5a9b8a   Interactive hover, ice accents
*/

/* --- Global --- */
* {
    font-family: "Cinzel", "Garamond", "Palatino Linotype", "Book Antiqua", serif;
    font-size: 14px;
}

QMainWindow {
    background-color: #0a0d0a;
    color: #d8c8a8;
}

QDialog {
    background-color: #0a0d0a;
    color: #d8c8a8;
}

QWidget {
    background-color: transparent;
    color: #d8c8a8;
}

QMainWindow > QWidget#centralWidget {
    background-color: #0a0d0a;
}

/* --- Menu Bar --- */
QMenuBar {
    background-color: #0a0d0a;
    color: #6a7a6a;
    border-bottom: 1px solid #2a352a;
    padding: 2px 4px;
    font-size: 12px;
}

QMenuBar::item {
    padding: 5px 14px;
    background: transparent;
    border-radius: 3px;
}

QMenuBar::item:selected {
    background-color: #1c241c;
    color: #5a9b8a;
}

QMenu {
    background-color: #121812;
    color: #d8c8a8;
    border: 1px solid #2a352a;
    border-radius: 4px;
    padding: 4px;
}

QMenu::item {
    padding: 7px 28px 7px 14px;
    border-radius: 3px;
}

QMenu::item:selected {
    background-color: #1c241c;
    color: #5a9b8a;
}

QMenu::separator {
    height: 1px;
    background-color: #2a352a;
    margin: 4px 10px;
}

/* --- Tab Widget --- */
QTabWidget::pane {
    background-color: #0a0d0a;
    border: 1px solid #2a352a;
    border-top: none;
    border-bottom-left-radius: 6px;
    border-bottom-right-radius: 6px;
    padding: 2px;
}

QTabBar {
    background: transparent;
}

QTabBar::tab {
    background-color: #121812;
    color: #6a7a6a;
    padding: 10px 28px;
    margin-right: 2px;
    border: 1px solid #2a352a;
    border-bottom: none;
    border-top-left-radius: 6px;
    border-top-right-radius: 6px;
    font-family: "Cinzel", "Perpetua Titling MT", "Garamond", serif;
    font-weight: bold;
    font-size: 13px;
    min-width: 100px;
}

QTabBar::tab:selected {
    background-color: #0a0d0a;
    color: #d4a017;
    border: 1px solid #2a352a;
    border-bottom: none;
    border-top: 3px solid qlineargradient(x1:0, y1:0, x2:1, y2:0,
        stop:0 #3a2a0a, stop:0.3 #d4a017, stop:0.5 #f0c030, stop:0.7 #d4a017, stop:1 #3a2a0a);
    padding-top: 8px;
}

QTabBar::tab:hover:!selected {
    background-color: #1c241c;
    color: #5a9b8a;
}

/* --- Buttons: Default Flat --- */
QPushButton {
    background-color: #121812;
    color: #d8c8a8;
    border: 1px solid #2a352a;
    border-radius: 5px;
    padding: 7px 18px;
    min-height: 24px;
    font-weight: bold;
}

QPushButton:hover {
    background-color: #1c241c;
    border-color: #5a9b8a;
    color: #5a9b8a;
}

QPushButton:pressed {
    background-color: #0a0d0a;
    border-color: #5a9b8a;
}

QPushButton:disabled {
    background-color: #0a0d0a;
    color: #3a423a;
    border-color: #1a221a;
}

/* --- Buttons: Small toolbar --- */
QPushButton#btn_toolbar {
    padding: 2px;
    min-height: 0px;
}

/* --- Buttons: Editor toolbar (B/I/U/fold) --- */
QPushButton#btn_editor_toolbar {
    padding: 7px 8px;
}

/* --- Buttons: Gold Accent --- */
QPushButton#btn_gold {
    background-color: #121812;
    border: 2px solid #d4a017;
    color: #d4a017;
    font-weight: bold;
    font-size: 13px;
    font-family: "Cinzel", "Perpetua Titling MT", "Garamond", serif;
}

QPushButton#btn_gold:hover {
    background-color: #1c241c;
    border-color: #f0c030;
    color: #f0c030;
}

QPushButton#btn_gold:pressed {
    background-color: #0a0d0a;
    border-color: #f0c030;
}

/* --- Buttons: Record (red accent on same flat base) --- */
QPushButton#btn_record {
    background-color: #121812;
    border: 2px solid #6b2a2a;
    color: #d44a4a;
    font-size: 13px;
    font-weight: bold;
    padding: 6px 14px;
    border-radius: 6px;
    min-height: 24px;
}

QPushButton#btn_record:hover {
    background-color: #1c241c;
    border-color: #d44a4a;
    color: #e65c5c;
}

QPushButton#btn_record:pressed {
    background-color: #0a0d0a;
    border-color: #d44a4a;
}

/* --- Buttons: Pause (amber accent) --- */
QPushButton#btn_pause {
    background-color: #121812;
    border: 2px solid #7a5a1a;
    color: #e8a824;
    font-size: 13px;
    font-weight: bold;
    padding: 6px 14px;
    border-radius: 6px;
    min-height: 24px;
}

QPushButton#btn_pause:hover {
    background-color: #1c241c;
    border-color: #e8a824;
    color: #f0be40;
}

QPushButton#btn_pause:pressed {
    background-color: #0a0d0a;
    border-color: #e8a824;
}

/* --- Buttons: Resume (green accent) --- */
QPushButton#btn_resume {
    background-color: #121812;
    border: 2px solid #2a5a2a;
    color: #4caf50;
    font-size: 13px;
    font-weight: bold;
    padding: 6px 14px;
    border-radius: 6px;
    min-height: 24px;
}

QPushButton#btn_resume:hover {
    background-color: #1c241c;
    border-color: #4caf50;
    color: #66cc6a;
}

QPushButton#btn_resume:pressed {
    background-color: #0a0d0a;
    border-color: #4caf50;
}

/* --- Buttons: Stop --- */
QPushButton#btn_stop {
    background-color: #121812;
    border: 2px solid #5a9b8a;
    color: #5a9b8a;
    font-size: 13px;
    font-weight: bold;
    padding: 6px 14px;
    border-radius: 6px;
    min-height: 24px;
}

QPushButton#btn_stop:hover {
    background-color: #1c241c;
    border-color: #7ab8a8;
    color: #7ab8a8;
}

/* --- Buttons: Primary (same as default, frost-blue accent) --- */
QPushButton#btn_primary {
    background-color: #121812;
    border: 1px solid #5a9b8a;
    color: #5a9b8a;
    font-weight: bold;
}

QPushButton#btn_primary:hover {
    background-color: #1c241c;
    border-color: #7ab8a8;
    color: #7ab8a8;
}

/* --- Buttons: Overflow "more" menu --- */
QToolButton#btn_more {
    background-color: #121812;
    border: 1px solid #2a352a;
    border-radius: 5px;
    color: #6a7a6a;
    font-size: 18px;
    font-weight: bold;
    padding: 4px 10px;
    min-height: 24px;
}

QToolButton#btn_more:hover {
    background-color: #1c241c;
    border-color: #5a9b8a;
    color: #5a9b8a;
}

QToolButton#btn_more::menu-indicator {
    image: none;
    width: 0;
}

/* --- Text Edits --- */
QTextEdit, QPlainTextEdit {
    background-color: #0a0d0a;
    color: #d8c8a8;
    border: 1px solid #2a352a;
    border-radius: 6px;
    padding: 10px;
    selection-background-color: #1c241c;
    selection-color: #5a9b8a;
}

/* Journal Editor */
QTextEdit#journal_editor {
    background-color: #1a120a;
    border: 1px solid #3a2a1a;
    border-left: 3px solid #d4a017;
    border-radius: 6px;
    font-size: 14px;
    color: #d8c8a8;
    padding: 14px 18px;
}

/* Quest Log Editor */
QTextEdit#quest_log_editor {
    background-color: #1a120a;
    border: 1px solid #3a2a1a;
    border-left: 3px solid #d4a017;
    border-radius: 6px;
    font-size: 14px;
    color: #d8c8a8;
    padding: 14px 18px;
}

/* Summary Display */
QTextEdit#summary_display {
    background-color: #1a120a;
    border: 1px solid #3a2a1a;
    border-left: 3px solid #d4a017;
    color: #d8c8a8;
    padding: 14px 18px;
    border-radius: 6px;
}

/* Transcript Display */
QTextEdit#transcript_display {
    background-color: #080a08;
    color: #6a7a6a;
    font-family: "Consolas", "Courier New", monospace;
    font-size: 12px;
    border: 1px solid #2a352a;
    border-left: 3px solid #5a9b8a;
    padding: 12px 14px;
    border-radius: 6px;
}

/* --- Line Edits --- */
QLineEdit {
    background-color: #0a0d0a;
    color: #d8c8a8;
    border: 1px solid #2a352a;
    border-radius: 5px;
    padding: 6px 12px;
    min-height: 26px;
}

QLineEdit:focus {
    border-color: #5a9b8a;
}

/* --- ComboBox --- */
QComboBox {
    background-color: #121812;
    color: #d8c8a8;
    border: 1px solid #2a352a;
    border-radius: 5px;
    padding: 6px 12px;
    min-height: 26px;
}

QComboBox:hover {
    border-color: #5a9b8a;
}

QComboBox::drop-down {
    border: none;
    width: 26px;
}

QComboBox QAbstractItemView {
    background-color: #121812;
    color: #d8c8a8;
    border: 1px solid #2a352a;
    selection-background-color: #1c241c;
    selection-color: #5a9b8a;
    border-radius: 4px;
    padding: 4px;
}

/* --- Splitter --- */
QSplitter::handle {
    background-color: #2a352a;
    width: 4px;
}

QSplitter::handle:hover {
    background-color: #5a9b8a;
}

/* --- ScrollBars --- */
QScrollBar:vertical {
    background-color: #0a0d0a;
    width: 10px;
    margin: 0;
    border-radius: 5px;
}

QScrollBar::handle:vertical {
    background-color: #2a352a;
    min-height: 40px;
    border-radius: 5px;
    margin: 2px;
}

QScrollBar::handle:vertical:hover {
    background-color: #354535;
}

QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical,
QScrollBar::add-page:vertical, QScrollBar::sub-page:vertical {
    height: 0;
    background: none;
}

QScrollBar:horizontal {
    background-color: #0a0d0a;
    height: 10px;
    margin: 0;
    border-radius: 5px;
}

QScrollBar::handle:horizontal {
    background-color: #2a352a;
    min-width: 40px;
    border-radius: 5px;
    margin: 2px;
}

QScrollBar::handle:horizontal:hover {
    background-color: #354535;
}

QScrollBar::add-line:horizontal, QScrollBar::sub-line:horizontal,
QScrollBar::add-page:horizontal, QScrollBar::sub-page:horizontal {
    width: 0;
    background: none;
}

/* --- Progress Bar --- */
QProgressBar {
    background-color: #0a0d0a;
    border: 1px solid #2a352a;
    border-radius: 6px;
    text-align: center;
    color: #d8c8a8;
    min-height: 14px;
    max-height: 14px;
}

QProgressBar::chunk {
    background: qlineargradient(x1:0, y1:0, x2:1, y2:0,
        stop:0 #5a9b8a, stop:0.5 #d4a017, stop:1 #b85a20);
    border-radius: 4px;
    margin: 2px;
}

/* --- Labels --- */
QLabel {
    color: #d8c8a8;
    background: transparent;
}

QLabel#heading {
    color: #d4a017;
    font-family: "Cinzel", "Perpetua Titling MT", "Garamond", "Palatino Linotype", serif;
    font-size: 24px;
    font-weight: bold;
    padding: 6px;
}

QLabel#subheading {
    color: #5a9b8a;
    font-family: "Cinzel", "Garamond", "Palatino Linotype", serif;
    font-size: 15px;
    font-weight: bold;
    padding: 3px 0px;
}

QLabel#status_label {
    color: #6a7a6a;
    font-style: italic;
    padding: 4px 8px;
    font-size: 12px;
}

QLabel#duration_label {
    color: #5a9b8a;
    font-size: 22px;
    font-weight: bold;
    font-family: "Consolas", "Courier New", monospace;
    padding: 2px 8px;
}

QLabel#divider_label {
    background: transparent;
    min-height: 12px;
    max-height: 12px;
}

/* --- Tool Bar --- */
QToolBar {
    background-color: #0a0d0a;
    border-bottom: 1px solid #2a352a;
    spacing: 4px;
    padding: 2px;
}

QToolButton {
    background-color: transparent;
    color: #6a7a6a;
    border: 1px solid transparent;
    border-radius: 4px;
    padding: 5px 10px;
}

QToolButton:hover {
    background-color: #1c241c;
    color: #5a9b8a;
}

QToolButton:pressed {
    background-color: #0a0d0a;
}

/* --- Group Box --- */
QGroupBox {
    border: 1px solid #2a352a;
    border-radius: 6px;
    margin-top: 16px;
    padding: 18px 12px 12px 12px;
    background-color: #121812;
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left;
    padding: 3px 14px;
    color: #d4a017;
    font-family: "Cinzel", "Garamond", serif;
    font-weight: bold;
    font-size: 13px;
    background-color: #121812;
    border: 1px solid #2a352a;
    border-radius: 3px;
}

/* --- Tooltips --- */
QToolTip {
    background-color: #121812;
    color: #d8c8a8;
    border: 1px solid #5a9b8a;
    padding: 6px 10px;
    border-radius: 4px;
    font-size: 12px;
}

/* --- SpinBox --- */
QSpinBox, QDoubleSpinBox {
    background-color: #0a0d0a;
    color: #d8c8a8;
    border: 1px solid #2a352a;
    border-radius: 5px;
    padding: 5px;
}

QSpinBox:focus, QDoubleSpinBox:focus {
    border-color: #5a9b8a;
}

/* --- CheckBox --- */
QCheckBox {
    color: #d8c8a8;
    spacing: 8px;
}

QCheckBox::indicator {
    width: 18px;
    height: 18px;
    border: 1px solid #2a352a;
    border-radius: 3px;
    background-color: #0a0d0a;
}

QCheckBox::indicator:checked {
    background-color: #5a9b8a;
    border-color: #5a9b8a;
}

QCheckBox::indicator:hover {
    border-color: #5a9b8a;
}

/* --- Search Bar --- */
QWidget#search_bar {
    background-color: #121812;
    border-top: 1px solid #2a352a;
}

QWidget#search_bar QLineEdit {
    min-height: 22px;
    font-size: 13px;
}

QWidget#search_bar QPushButton {
    min-height: 20px;
    padding: 2px 6px;
    font-size: 12px;
}

/* --- Fold Gutter --- */
FoldGutterWidget {
    background-color: #0a0d0a;
}

/* --- Dialog Buttons --- */
QDialogButtonBox QPushButton {
    min-width: 100px;
}
